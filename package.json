{
  "name": "compara-lista-electron",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "build": "electron-packager . compara-listas --platform=win32 --arch=x64 --overwrite"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^21.1.0",
    "electron-packager": "^16.0.0",
    "electron-rebuild": "^3.2.9"
  }
}
